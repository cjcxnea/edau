## 提示词

首先，充分理解所有的文件结构和代码内容，忽略掉note.md文件，此文件不参与任何功能实现。

#### 给v1.html增加查询验证功能

1. 用户在考试科目下拉选择框选择的信息，与文件夹json/user.json中的testSub数据进行匹配验证
2. 用户在姓名输入框输入的信息，与文件夹json/user.json中的uname数据进行匹配验证
3. 用户在证件号码/准考证号输入框输入的信息，与文件夹json/user.json中的idCard或者examNum其中任意一个数据进行匹配验证
4. 以上验证都通过后，用户点击.btn button查询按钮，跳转到score.html页面,同时不需要将用户信息存储到localStorage中。验证失败时，提示用户输入正确的信息
5. 用于新增以上功能的js代码写入到js/index.js文件中，并在js代码中加上注释说明

#### 修改score.js中的问题

1. score.js是给score.html页面使用的，用于渲染用户信息，敏感信息脱敏以及返回index.html页面，所有的改动都必须基于已有的代码进行
2. 页面加载时，直接将文件夹json/user.json中的用户信息一一对应的渲染到score.html页面的对应位置，需要渲染的内容有：
   1. #test-sub 对应 testSub数据
   2. #user-name 对应 uname数据
   3. #user-idcard 对应 idCard数据
   4. #user-school 对应 schoolName数据
   5. #score-exam-num 对应 examNum数据
   6. #score-total 对应 totalScore数据
   7. #score-listening 对应 listening数据
   8. #score-reading 对应 reading数据
   9. #score-writing 对应 writing数据
   10. #speaking-exam-num 对应 speakingExamNum数据
   11. #speaking-score 对应 speakingScore数据
3. 每次加载页面时，都需要从文件夹json/user.json中读取用户信息，根据用户输入的查询信息，从用户信息中提取出对应的用户信息，渲染到score.html页面的对应位置，不需要存储到localStorage中
4. 必须做到已经完成数据渲染的信息，在页面刷新后，仍然能够显示在score.html页面的对应位置
5. score.js已有的从localStorage中读取用户信息的代码，需要调整以满足以上要求
6. 改动的js代码都加上注释说明

#### 增加新功能
1. 首先分析所有的文件，了解他们的结构和内容
2. 我要将我们班级里30名同学的考试信息全部记录在文件user.json中，格式是现有的固定格式，不可以改动，否则会影响其他功能的实现
3. 有没有什么方案，能够让我快速的将所有人的信息分别以现有的格式写入到user.json文件中，且不能影响现有的页面功能和样式
4. 我自己的方案一：我可以使用python语言，编写一个脚本，读取文件夹json/user.json中的数据，将其写入到user.json文件中，每个用户信息之间用逗号隔开，且每个用户信息的字段顺序与json/user.json中的字段顺序相同
5. 除了方案一之外，还有其他的方案吗？将所有的方案给我介绍一下各自的特点，等我选择后再执行


我觉得方案一，方案二，方案五都比较适合我，，你分别按照这三种方案执行
必须保证不同的方案之间不能有冲突，否则会影响其他功能的实现

1. 经过我的使用和对比，我决定只留下方案一，其他方案都不继续使用，并且删除了相关文件
2. 为了更好的使用方案一，我将json/user.json中的数据结构觉醒了微调，以便更加方便的写入或导出新的用户信息，同时其他文件也进行了微调，在开始新的任务之前先重新分析所有的json文件、Python文件、HTML文件、JS文件，其他不参与功能实现的文件忽略掉即可
3. 对scripts/add_users.py文件新增一项功能：可以将json/user.json中的用户信息导出到已有的csv文件中。
4. 同时根据json数据中的编号字段和姓名字段检查数据是否已经存在，若已存在，则不再写入到csv文件中，避免重复数据
5. 是否可以根据Python文件里所展示的功能，给出一个可视化的界面，用于新增用户信息到json/user.json文件中，同时可以将json/user.json中的用户信息导出到已有的csv文件中

#### 新增设备检测功能
1. 用于电脑端页面显示的css文件有
   - common-dt.css
   - index-dt.css
   - score-dt.css
2. 用于移动端页面显示的css文件有
   - common-m.css
   - index-m.css
   - score-m.css
3. 在js/内，新建一个用于设备检测的js文件，只检测打开页面的设备是电脑端还是移动端，不检测屏幕大小的变化
4. 电脑端打开时，页面引入用于电脑端页面显示的css文件
   - index.html引入common-dt.css、index-dt.css
   - score.html引入common-dt.css、score-dt.css
5. 移动端打开时，页面引入用于移动端页面显示的css文件
   - index.html引入common-m.css、index-m.css
   - score.html引入common-m.css、score-m.css
6. 用于设备检测的js文件，同时被index.html和score.html引入
7. 其他无关的代码不要改动

通过js代码进行设备检测，会影响页面的加载速度和性能吗，如果影响，根据我的页面的实际情况，给我个优化性能的方案，等我确认后，就按照方案执行


#### 增加注释

1. 先整体分析一下js/文件夹中的所有文件，并理解他们的功能特点
2. 给每个js文件中的代码加上注释，方便我后续维护
3. 每段代码片段都要加上注释，说明其功能

#### 新增隐藏成绩报告单编号的功能

1. 当用户完成查询验证之后，将user.json中的数据渲染到score.html时，只要user.json中的reportNum字段的值为0，就给score.html中的 .user-info li:nth-child(4) 添加 display: none; 隐藏起来
2. 其他的内容和功能不变，继续保持原有的功能和样式

#### json文件数据处理

1. json/user-info-25(下).json文件是我之前记录的数据，现在要将它进行处理，以便能够让当前的score.html正常的渲染数据
2. 我的思路是：先将user-info-25(下).json中的所有数据写入到sample_users.csv，覆盖掉sample_users.csv中的所有数据，再对user-info-25(下).json中原本没有的字段进行补全，"No."字段从1开始依次递增，"speakingExamNum"和"speakingScore"的值都设为"--"
3. 你可以评估一下我的思路是否可行和便捷，你也可以提出自己的想法，等我确认后方可执行

#### add_users.py增加新功能

1. 增加导出用户数据到Excel文件的函数模块，函数命名，及结构与其他几个函数模块保持一致，且不能冲突
2. 给export_users_to_csv函数增加新功能：在导出数据时，在现有的追加写入的基础上，增加可以选择全部覆盖写入的选项
   - 如果用户选择了全部覆盖写入，那么就会将sample_users.csv中的所有数据全部删除，并用json/user-25(下).json中的数据替换
   - 如果用户选择了追加写入，那么就会在sample_users.csv的末尾追加写入json/user-25(下).json中的数据
   - 写入时的查重规则保持不变
3. 将所有的函数的参数json_file_path='json/user-25(下).json'中的路径，变成一个变量，方便后续的维护和修改

#### 给data_analysis.ipynb增加绘制统计图的功能

1. 根据字段idCard的倒数第二位数的奇偶性，奇术为男性，偶数为女性，统计所有的用户的性别比例
2. 根据统计结果，利用 import matplotlib.pyplot as plt 绘制一个环形统计图，展示所有用户的性别比例
   - 环形统计图的标题为"用户性别比例"
   - 环形统计图的图例为"男性"和"女性"
   - 环形统计图的颜色为"#66b3ff"和"#ff9999"
   - 环形统计图的百分比显示格式为"%.1f%%"
   - 环形统计图的百分比标签字体大小为12
   - 环形统计图的百分比标签字体颜色为"white"
   - 环形统计图的百分比标签字体加粗
   - 环形统计图的百分比标签与圆心的距离为0.3
   - 环形统计图的百分比标签字体居中对齐
3. 根据字段idCard的前两位数，判断用户所在的省份，不同的前两位代表一个省份/自治区/直辖市，统计所有用户的省份分布地区
4. 绘制一张用户分布地图统计图，在中国地图上，展示不同的省份分别都有几名用户
5. 在地图上，用深浅不同的颜色表示用户分布数量的多与少，数量越多的省份，颜色越深，代表用户数量越多
6. 地图上的每个省份，都要显示其名称和用户数量，如果没有用户分布，也需要显示其名称，只是用户数量为0
   - 该统计图的标题为"用户分布地图"
   - 该统计图的字体大小为12
   - 该统计图的字体颜色为"black"
   - 该统计图的字体加粗
   - 该统计图的背景颜色为"white"

以上所有的操作都按照我已经编辑好的ipunv文件中进行，且不要改动我已经编辑好的数据统计内容，按照我的编辑喜欢继续进行即可

1. 先分析这段代码的上下文，理解基本情况后再用改动最小的方式进行调整
2. 将用户分布地图上的渐变颜色变更一下，由现在的颜色变为蓝色渐变的效果，由浅蓝色逐渐过渡到深蓝色
3. 改动完成后，并给这段代码的每一行添加上只是说明
4. 其他模块的代码保持不变

1. 充分理解本项目中除了.md文件之外的所有的文家内容，分析他们的结构和所在位置，先对本羡慕有一个初步的了解
2. 在现有的文件内容下，给出本项目还有什么地方可以优化和注意点地方，提出可行性的方案供我选择